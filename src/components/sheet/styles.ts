import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  handleStyle: { borderTopLeftRadius: 10, borderTopRightRadius: 10 },
  sheetContainer: {
    // add horizontal space
    marginHorizontal: 16
  }
});
